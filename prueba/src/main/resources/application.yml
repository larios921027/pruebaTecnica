spring:
  jpa:
   hibernate:
      ddl-auto: update
   properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/empresa?zeroDataTimeBehavior=convertToNull&serverTimezona=UTC
    username: root
    password: root